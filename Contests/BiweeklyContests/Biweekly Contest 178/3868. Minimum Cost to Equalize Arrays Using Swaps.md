# 3868. Minimum Cost to Equalize Arrays Using Swaps

**Difficulty:** Medium
**Date Solved:** 2026-03-14
**LeetCode Link:** [Link](https://leetcode.com/submissions/detail/1948098595/)

## Solution
```java
// Your code here
class Solution {
    public int minCost(int[] nums1, int[] nums2) {
        int arr[]=new int[100000];
        int eq[]=new int[100000];
        for(int i=0;i<nums1.length;i++){
            arr[nums1[i]]++;
            eq[nums1[i]]++;
            arr[nums2[i]]++;
        }
        // Arrays.sort(nums1);
        // Arrays.sort(nums2);
        int array[]=new int[nums1.length];
        int idx=0;
        for(int i=0;i<100000;i++){
            if(arr[i]%2!=0){
                return -1;
            }
            // int val=arr[i]/2;
            // while(val>0){
            //     array[idx++]=i;
            //     val--; 
            // }
        }
        int cnt=0;
        for(int i=0;i<100000;i++){
            // if(array[i]!=nums1[i]){
            //     cnt++;
            // }
            cnt+=Math.abs(arr[i]/2-eq[i]);
        }
        return cnt/2;
    }
}