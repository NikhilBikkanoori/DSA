# 3567. Minimum Absolute Difference in Sliding Submatrix

**Difficulty:** Medium
**Topics:**  Matrix,Sorting
**Date Solved:** 2026-03-20
**LeetCode Link:** [Link](https://leetcode.com/problems/minimum-absolute-difference-in-sliding-submatrix/description/)
## Solution
```java
// Your code here
class Solution {
    public int[][] minAbsDiff(int[][] grid, int k) {
        int m = grid.length;
        int n = grid[0].length;
        int[][] result = new int[m - k + 1][n - k + 1];
        for (int i = 0; i <= m - k; i++) {
            for (int j = 0; j <= n - k; j++) {
                TreeSet<Integer> vals = new TreeSet<>();
                for (int r = i; r <= i + k - 1; r++) {
                    for (int c = j; c <= j + k - 1; c++) {
                        vals.add(grid[r][c]);
                    }
                }
                if (vals.size() == 1) {
                    continue;
                }
                int min = Integer.MAX_VALUE;
                Integer prev = null;
                for (int val : vals) {
                    if (prev != null) {
                        min = Math.min(min, val - prev);
                    }
                    prev = val;
                }
                result[i][j] = min;
            }
        }
        return result;
    }
}
